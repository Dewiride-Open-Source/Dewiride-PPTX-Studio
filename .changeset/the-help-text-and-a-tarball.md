---
'@pptx-studio/cli': patch
'@pptx-studio/render-dom': patch
---

The help text spells `this platform's own font directories` with its apostrophe, and the
`render-dom` tarball carries `CHANGELOG.md` like the other eleven.
