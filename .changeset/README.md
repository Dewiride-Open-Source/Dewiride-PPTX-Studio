# Changesets

Run `pnpm changeset` to add one. Each file here describes a change to one or more published
packages and the version bump it deserves; `pnpm version-packages` consumes them.

Changes confined to `tools/`, `docs/` or CI do not need a changeset.
