# Registry/auth only. All pnpm behaviour settings live in pnpm-workspace.yaml
# (pnpm 10+ reads them from there); duplicating them here would create two
# sources of truth that drift.
registry=https://registry.npmjs.org/
